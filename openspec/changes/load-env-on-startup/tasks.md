## 1. Модуль парсера .env

- [x] 1.1 Создать `src/env/parse-env.ts` — функция парсинга строк `.env` в объект
- [x] 1.2 Реализовать обработку кавычек (удаление внешних `"` из значений)
- [x] 1.3 Реализовать игнорирование комментариев (`#`) и пустых строк
- [x] 1.4 Реализовать обработку строк с пробелами в кавычках
- [x] 1.5 Написать юнит-тесты для парсера (базовые случаи, кавычки, комментарии, пустые значения)

## 2. Модуль загрузки .env

- [x] 2.1 Создать `src/env/loader.ts` — функция загрузки `.env` файла
- [x] 2.2 Реализовать поиск `.env` в текущей рабочей директории (`process.cwd()`)
- [x] 2.3 Реализовать проверку существования файла перед чтением
- [x] 2.4 Реализовать логику приоритета: не перезаписывать существующие переменные
- [x] 2.5 Реализовать обработку ошибок (невозможность чтения, некорректный формат)
- [x] 2.6 Написать юнит-тесты для лоадера (файл есть/нет, приоритет переменных, ошибки)

## 3. Интеграция в точки входа

- [x] 3.1 Вызвать функцию загрузки `.env` в начале `src/index.ts` (до роутинга CLI/MCP)
- [x] 3.2 Добавить опциональное логирование факта загрузки (debug-level)
- [x] 3.3 Убедиться, что оба режима (CLI и MCP сервер) получают переменные

## 4. Предупреждения о конфигурации

- [x] 4.1 Создать `src/env/recommended.ts` — список рекомендуемых переменных из `.env.example`
- [x] 4.2 Реализовать функцию проверки установленных переменных
- [x] 4.3 Вызывать предупреждения при запуске (только если переменные не установлены)
- [x] 4.4 Добавить опцию отключения предупреждений (`RAG_QUIET_CONFIG=1`)

## 5. Автоматическое добавление .env в .gitignore

- [x] 5.1 При первой загрузке `.env` проверять наличие `.env` в `.gitignore`
- [x] 5.2 Если `.env` отсутствует в `.gitignore`, добавить его автоматически
- [x] 5.3 Добавить предупреждение о добавлении `.env` в `.gitignore`

## 6. Документация и тестирование

- [x] 6.1 Обновить `.env.example` — добавить комментарий о автоматической загрузке
- [x] 6.2 Обновить README.md — добавить секцию о `.env` файле
- [x] 6.3 Обновить README_RUS.md — добавить русскую версию описания
- [x] 6.4 Провести ручное тестирование (CLI, MCP сервер, различные сценарии)
- [x] 6.5 Запустить полный набор тестов (`pnpm run test`)
